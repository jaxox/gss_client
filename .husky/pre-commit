#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for code formatting and linting
npx lint-staged

# Run type checking on all packages
npm run type-check

# Run tests in CI mode (non-interactive, exit after completion)
npm run test:ci
