#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run lint-staged for code formatting and linting
npx lint-staged

# Run type checking on all packages
npm run type-check

# Run tests in CI mode
npm run test:ci

# E2E tests for iOS - Disabled for now as they should run separately
# cd mobile && npm run test:e2e:ios:debug
